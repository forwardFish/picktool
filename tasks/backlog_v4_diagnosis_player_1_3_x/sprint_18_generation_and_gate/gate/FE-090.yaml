project: "familyEducation"
task_id: "FE-090"
task_name: "Slide Action Generation And Deck Quality Gate"
sprint: "Sprint 18 Generation And Gate"
epic: "Deck Gate And TTS Policy"
story_id: "FE-090"
auto_run: true
execution_policy: "continuous_full_sprint"
interaction_policy: "non_interactive_auto_run"
pause_policy: "story_boundary_or_shared_blocker_only"
agent_policy: "auto"
blast_radius: "L2"
execution_mode: "Safe"
mode: "Safe"
story_type: "ordinary_story"
session_policy: "api_and_data_required"
auth_expectations: "report_auth_required"
workflow_enforcement_policy: "strict_agentsystem"
upstream_agent_parity: "required"
approval_required: false
goal: "Generate safe slide actions, validate anchors and references, and assign a visible A/B/C/D deck tier with degrade behavior."
business_value: "Prevents weak diagnosis evidence from presenting itself as a polished autoplay walkthrough."
requirement_ids:
  - "DECK13-TTS"
  - "DECK13-GATE"
test_case_ids:
  - "AI-DCK-003"
  - "AI-DCK-004"
  - "AI-DCK-006"
  - "AI-DCK-009"
  - "API-DCK-004"
expected_evidence:
  - "action_whitelist_output"
  - "deck_quality_score_output"
  - "deck_tier_degrade_assertion"
  - "regenerate_revalidation_output"
required_modes:
  - "plan-eng-review"
  - "qa"
  - "review"
  - "code_acceptance"
  - "acceptance_gate"
  - "doc_writer"
entry_criteria:
  - "Dependencies accepted: FE-089."
acceptance_criteria:
  - "Actions are limited to a documented whitelist and every action references a valid slide or evidence anchor."
  - "Decks are scored into A/B/C/D and their autoplay visibility follows the degrade rules."
  - "Regenerate slide and regenerate actions both re-run validation and update the persisted tier."
constraints:
  - "Voice guidance defaults to off and may not bypass the degrade policy."
out_of_scope:
  - "Parent/share route shells and admin review UX."
dependencies:
  - "FE-089"
preview_base_url: "http://127.0.0.1:3000"
preview_urls:
  - "http://127.0.0.1:3000/dashboard/reports/[reportId]"
qa_urls:
  - "http://127.0.0.1:3000/api/decks/[deckId]/regenerate-actions"
related_files:
  - "lib/family/*deck*"
  - "app/api/decks/[deckId]/regenerate-slide/route.ts"
  - "app/api/decks/[deckId]/regenerate-actions/route.ts"
primary_files:
  - "lib/family/*deck*"
  - "app/api/decks/[deckId]/regenerate-slide/route.ts"
  - "app/api/decks/[deckId]/regenerate-actions/route.ts"
secondary_files:
  - "app/api/decks/[deckId]/playback/route.ts"
test_cases:
  normal:
    - "Generate actions for a strong deck and confirm the playback contract stays within the whitelist."
  exception:
    - "Downgrade or hide the play entry when the deck lacks valid anchors or falls below the D-tier threshold."
