project: "familyEducation"
task_id: "FE-140"
task_name: "1.7 Start Diagnosis And Publish Contract"
sprint: "Sprint 30 Publish Contract And Data Foundations"
epic: "Publish Contract"
story_id: "FE-140"
auto_run: true
execution_policy: "continuous_full_sprint"
interaction_policy: "non_interactive_auto_run"
pause_policy: "story_boundary_or_shared_blocker_only"
agent_policy: "auto"
blast_radius: "L2"
execution_mode: "Safe"
mode: "Safe"
story_type: "ordinary_story"
session_policy: "planning_assets_required"
auth_expectations: "program_level_review"
workflow_enforcement_policy: "strict_agentsystem"
upstream_agent_parity: "required"
approval_required: false
goal: "Expose the formal start-diagnosis API path and guarantee upload -> analysis run -> structured report publish remains the only workspace source of truth."
business_value: "Lets Overview, Members, and Reports resume real work from the same canonical diagnosis chain."
requirement_ids:
  - "DASH17-STATE"
test_case_ids:
  - "API-RUN-001"
  - "CLICK-003"
expected_evidence:
  - "start_diagnosis_route_smoke"
  - "structured_publish_chain_assertion"
required_modes:
  - "builder"
  - "qa"
  - "review"
acceptance_criteria:
  - "A POST start-diagnosis route exists for uploads."
  - "Completed runs publish the structured report objects consumed by dashboard pages."
constraints:
  - "Do not fork or replace the existing submit/process run pipeline."
primary_files:
  - "app/api/uploads/[uploadId]/start-diagnosis/route.ts"
  - "app/api/uploads/[uploadId]/submit/route.ts"
  - "lib/family/repository.ts"
secondary_files:
  - "components/uploads/upload-workspace.tsx"
test_cases:
  normal:
    - "Trigger start diagnosis and verify the returned run id transitions into the existing run detail flow."
  exception:
    - "Fail if the new route bypasses structured publish or returns a run without persisted report artifacts."
