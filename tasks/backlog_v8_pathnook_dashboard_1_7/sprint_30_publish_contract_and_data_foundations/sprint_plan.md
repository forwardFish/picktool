# Sprint 30 Plan

## Sprint Name
Publish Contract And Data Foundations

## Goal
Add the minimum schema, repository, and API foundations needed for the real dashboard workspace without forking the existing diagnosis pipeline.

## Exit Criteria
- Child notes and chat continuation schema are additive and wired into the mock/runtime path.
- A `start-diagnosis` API path exists alongside the current upload submit flow.
- The publish contract guarantees uploads become runs and structured report objects before pages read them.

## Delivery Order
1. FE-139
2. FE-140
3. FE-141
