# Sprint 31 Plan

## Sprint Name
Overview And Members Workspace

## Goal
Replace prototype members and dashboard hints with real overview/member view models, explicit empty states, and resume-driven actions.

## Exit Criteria
- `/dashboard` is powered by `get-overview-workspace`.
- `/dashboard/children` and `/dashboard/children/[childId]` are powered by `get-members-page-data`.
- No hardcoded member/report demo arrays remain on the logged-in workspace surfaces.

## Delivery Order
1. FE-142
2. FE-143
3. FE-144
