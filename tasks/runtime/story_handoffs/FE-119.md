# FE-119 Handoff

- Updated at: 2026-04-13T16:20:00+08:00
- Project: familyEducation
- Backlog: backlog_v6_freemius_primary_billing_1_4
- Sprint: sprint_26_provider_and_data_layer
- Story: FE-119
- Status: implemented

## Delivered
- Added pure entitlement projection rules in `lib/payments/entitlement-projection.ts`
- Routed snapshot authority through `lib/payments/entitlements.ts` and `lib/family/billing.ts`
- Explicitly covered `one_time`, `monthly`, `annual`, upgrade-to-annual, `scheduled_cancel`, `expired`, and `unpaid` style history-retention behavior in the projection model
- Ensured recurring entitlements snapshot historical unlocked report ids locally so expiration/cancellation does not silently wipe prior rights

## Validation
- `pnpm exec tsc --noEmit --pretty false`
- `npx tsx --test tests/billing-entitlements.test.ts`
- `pnpm build`

## Deferred
- Front-end route cutover to `/api/checkout`, `/api/portal`, and `/webhook` remains outside Sprint 26
- No public Pathnook branding/UI cutover is claimed in this handoff
